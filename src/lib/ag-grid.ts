/**
 * AG Grid theme configuration using CSS variables for consistent styling.
 * @example
 * const theme = themeQuartz.withParams(themeOM);
 */
export const themeOM = {
    borderColor: 'var(--color-border)',
    headerBackgroundColor: 'var(--color-gray-100)',
    headerFontWeight: '600',
    headerFontFamily: 'var(--font-roboto-slab)',
    rowHoverColor: 'var(--color-gray-50)',
    accentColor: 'var(--color-primary)',
    iconButtonBorderRadius: '100%',
    iconButtonHoverBackgroundColor: 'var(--color-primary-100)',
    iconButtonHoverColor: 'var(--color-primary-foreground)'
};